export const metadata = {
  title: 'Contact Us — TIYE',
  description: 'Get in touch with TIYE to volunteer, partner, sponsor programmes, or invite us to your community.',
};

export default function ContactLayout({ children }) {
  return children;
}
